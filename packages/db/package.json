{
  "name": "@tellann/db",
  "private": true,
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "dependencies": {
    "@prisma/client": "^5.20.0",
    "@tellann/shared": "workspace:*",
    "@tellann/rules": "workspace:*"
  },
  "devDependencies": {
    "prisma": "^5.20.0",
    "typescript": "^5.0.0"
  },
  "scripts": {
    "build": "prisma generate && tsc",
    "generate": "prisma generate",
    "push": "prisma db push",
    "seed:plans": "tsx src/seed-plans.ts",
    "seed:rulesets": "ts-node prisma/seeds/rulesets.ts",
    "seed:admin": "ts-node prisma/seeds/system-admin.ts"
  }
}
